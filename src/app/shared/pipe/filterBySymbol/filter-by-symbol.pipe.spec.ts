import { FilterBySymbolPipe } from './filter-by-symbol.pipe';

describe('FilterBySymbolPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterBySymbolPipe();
    expect(pipe).toBeTruthy();
  });
});
